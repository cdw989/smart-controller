<Page
    x:Class="smart_controller.views.MusicView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:smart_controller.views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="5*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="5*"/>
        </Grid.RowDefinitions>

        <Button x:Name="tv" HorizontalAlignment="Center" Height="120" Width="120" Click="home_Click">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="4*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <FontIcon FontFamily="Segoe MDL2 Assets" FontSize="24" Glyph="&#xE1C3;" />
                <TextBlock Text="Home" Grid.Row="1"/>
            </Grid>
        </Button>

        <Grid Grid.Column="1" Grid.Row="1">
            <TextBox Text="Music"/>
            <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE189;" />
        </Grid>
    </Grid>
</Page>
